{% extends "admin.layout.html.twig" %}
{% block stylesheet %}
{# Mettre ici le liens vers le CSS de la page #}
{% endblock %}

{% block body %}

<h1>Liste des intervenants</h1>

<table>
    <thead>
        <tr>
            <th>Nom de l'intervenant</th>
            <th>Prénom de l'intervenant</th>
            <th>Rôle de l'intervenant</th>
        </tr>
    </thead>

    <tbody>
        {% for intervenant in intervenants %}
        <tr>
            <td>{{ intervenant.nom }}</td>
            <td>{{ intervenant.prenom }}</td>
            <td>{{ intervenant.role }}</td>
            <td>
                <a href="{{ path('inter.delete', {id: intervenant.id}) }}">Supprimer</a>
            </td>
        </tr>
        {% endfor %}
    </tbody>
</table>

{% endblock %}